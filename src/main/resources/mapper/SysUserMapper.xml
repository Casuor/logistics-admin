<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aki.springbootlogisticsadmin.mapper.SysUserMapper">
    <select id="getMenuIds" resultType="java.lang.Long">
        select DISTINCT rm.menu_id
        from sys_user_role ur
                 left join sys_role_menu rm on ur.role_id = rm.role_id
        where ur.user_id = #{user_id}
    </select>
    <select id="listByMenuId" resultType="com.aki.springbootlogisticsadmin.entity.SysUser">
        select DISTINCT su.*
        from sys_user_role ur
                 left join sys_role_menu rm on ur.role_id = rm.role_id
                 left join sys_user su on ur.user_id = su.id
        where rm.menu_id = #{menu_id}
    </select>
</mapper>
